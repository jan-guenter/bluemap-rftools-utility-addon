name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  PROMOTION_JAVA_VERSION: '21.0.12+8.0.LTS'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Check out add-on
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          persist-credentials: false

      - name: Check out exact BlueMap backport
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: jan-guenter/BlueMap
          ref: 7e07f4e74ec1e92a6ead9aa1e66054af3e133aac
          path: .ci/bluemap
          submodules: recursive
          fetch-depth: 0
          persist-credentials: false

      - name: Verify exact detached BlueMap identities
        run: |
          test "$(git -C .ci/bluemap rev-parse --abbrev-ref HEAD)" = "HEAD"
          test "$(git -C .ci/bluemap rev-parse HEAD)" = \
            "7e07f4e74ec1e92a6ead9aa1e66054af3e133aac"
          test "$(git -C .ci/bluemap/api rev-parse HEAD)" = \
            "285c9a60eff3ac2b0cab308ce1058d1565be0971"

      - name: Set up exact promotion Java 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: ${{ env.PROMOTION_JAVA_VERSION }}

      - name: Set up Python 3.13
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.13'

      - name: Install exact BlueMap add-on toolkit
        run: |
          python -m pip install --disable-pip-version-check \
            --no-deps --require-hashes --only-binary=:all: \
            --requirement requirements/toolkit.txt
          test "$(bluemap-addon-toolkit --version)" = "0.3.0-alpha.1"

      - name: Set up Gradle 9.6.1
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          gradle-version: '9.6.1'
          cache-provider: basic
          cache-read-only: ${{ github.event_name == 'pull_request' }}
          validate-wrappers: true

      - name: Acquire exact ephemeral candidate inputs
        run: |
          input_dir="${RUNNER_TEMP}/exact-inputs"
          mkdir -p "${input_dir}"
          curl --fail --location --retry 3 --retry-all-errors \
            --output "${input_dir}/rftoolsutility-1.21-7.0.12.jar" \
            "https://www.curseforge.com/api/v1/mods/342466/files/8256212/download"
          test "$(stat -c %s "${input_dir}/rftoolsutility-1.21-7.0.12.jar")" -eq 1434987
          sha256sum --check <<SHA256SUMS
          1fdbf7505c6d6f4ef93b8b15961c9c1a6a4d35a5d676297f8f647916238f4d2a  ${input_dir}/rftoolsutility-1.21-7.0.12.jar
          SHA256SUMS
          curl --fail --location --retry 3 --retry-all-errors \
            --output "${input_dir}/mcjtylib-1.21-9.0.21.jar" \
            "https://www.curseforge.com/api/v1/mods/233105/files/8256201/download"
          test "$(stat -c %s "${input_dir}/mcjtylib-1.21-9.0.21.jar")" -eq 699982
          sha256sum --check <<SHA256SUMS
          b8eca900d4fe77a495c74137bd6be79c67281e5d7ae67ca55f59980c64960a0e  ${input_dir}/mcjtylib-1.21-9.0.21.jar
          SHA256SUMS
          curl --fail --location --retry 3 --retry-all-errors \
            --output "${input_dir}/rftoolsbase-1.21-6.0.11.jar" \
            "https://www.curseforge.com/api/v1/mods/326041/files/8256204/download"
          test "$(stat -c %s "${input_dir}/rftoolsbase-1.21-6.0.11.jar")" -eq 463973
          sha256sum --check <<SHA256SUMS
          5195ba530e6cf9ba61c9954a3297679e6d29aa1b6182e27ae14ea43463dd4b00  ${input_dir}/rftoolsbase-1.21-6.0.11.jar
          SHA256SUMS

      - name: Read project version
        id: version
        run: |
          version="$(sed -n 's/^addon_version=//p' gradle.properties)"
          test -n "${version}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          if [[ "${version}" == *-SNAPSHOT ]]; then
            echo "release_candidate=false" >> "${GITHUB_OUTPUT}"
          else
            echo "release_candidate=true" >> "${GITHUB_OUTPUT}"
          fi

      - name: Verify prototype
        if: steps.version.outputs.release_candidate == 'false'
        run: |
          input_dir="${RUNNER_TEMP}/exact-inputs"
          gradle --no-daemon \
            -PbluemapSourcePath=.ci/bluemap \
            -PrftoolsUtilityJar="${input_dir}/rftoolsutility-1.21-7.0.12.jar" \
            -PmcjtylibJar="${input_dir}/mcjtylib-1.21-9.0.21.jar" \
            -PrftoolsBaseJar="${input_dir}/rftoolsbase-1.21-6.0.11.jar" \
            clean prototypeCheck build

      - name: Verify reviewed release candidate
        if: steps.version.outputs.release_candidate == 'true'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          input_dir="${RUNNER_TEMP}/exact-inputs"
          gradle --no-daemon \
            -PbluemapSourcePath=.ci/bluemap \
            -PrftoolsUtilityJar="${input_dir}/rftoolsutility-1.21-7.0.12.jar" \
            -PmcjtylibJar="${input_dir}/mcjtylib-1.21-9.0.21.jar" \
            -PrftoolsBaseJar="${input_dir}/rftoolsbase-1.21-6.0.11.jar" \
            "-PreleaseTag=v${VERSION}" \
            clean build generatePomFileForAddonPublication \
            generateMetadataFileForAddonPublication verifyReleaseCandidate

      - name: Upload review outputs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bluemap-rftools-utility-addon-${{ github.sha }}
          path: |
            build/libs/*.jar
            build/publications/addon/pom-default.xml
            build/publications/addon/module.json
            build/reports/**
          if-no-files-found: error
          retention-days: 14
